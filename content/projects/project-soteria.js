// project-soteria
EXITZERO.project({
  slug:  'project-soteria',

  // what the button shows
  card: {
    tag:   'computer vision',
    desc:  'Detection over open water. Small-object tracking against a moving, ' +
           'low-contrast background — the case where most off-the-shelf detectors fall ' +
           'apart.',
    stack: ['Python', 'OpenCV', 'YOLO'],
  },

  // no `page` yet — opening this entry lands on the 404

});
